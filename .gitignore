# Dependencies
node_modules/

# Environment / secrets — NEVER commit real values (spec §4: secrets in env only)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Build output
dist/
build/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Postgres volume (if bind-mounted locally)
pgdata/

# Embedded-Postgres test artifacts (server/test/*.test.cjs creates these per run)
server/test/.tmp/
logfile
