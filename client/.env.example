# Copy to .env.production (or set directly in your hosting provider's dashboard).
# Unset locally — the Vite dev server proxies /api to the backend (see vite.config.ts).

# Absolute origin of the deployed API (no trailing slash, no /api suffix), e.g.
# https://saferoute-tms-api.onrender.com
VITE_API_BASE_URL=
